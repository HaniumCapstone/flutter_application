import 'package:flutter/material.dart';

class KakaoMapPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Kakao Map을 띄우는 코드를 작성하세요.
    // 이 페이지에서 Kakao Map을 표시하고 원하는 기능을 구현하세요.
    return Scaffold(
      appBar: AppBar(
        title: Text('Kakao Map'),
      ),
      body: Center(
        child: Text('Kakao Map을 표시하는 페이지입니다.'),
      ),
    );
  }
}
